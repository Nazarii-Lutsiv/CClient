package parsing_features.current_command_parsers;

import parsing_features.CommandParser;
import parsing_features.ModelCommand;

public class PingCommandParser implements CommandParser {

    @Override
    public ModelCommand parsCommand(String regex) {
        return null;
    }
}
